# Just for example, here is a possible content for the file based on typical CI pipeline configurations.
# Add [on: workflow_call] if you want this workflow to be reusable.

name: CI Pipeline

on:
  push:
    branches: ["main"]

jobs:
  # lint:
  #   uses: ./.github/workflows/lint.yml

  # test:
  #   needs: lint
  #   uses: ./.github/workflows/test.yml

  # coverage:
  #   needs: test
  #   uses: ./.github/workflows/coverage.yml

  # Placeholder job to ensure the workflow is valid even if other jobs are commented out
  keep-going:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder Step
        run: echo "This is a placeholder job to keep the workflow valid."
